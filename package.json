{
  "name": "proofagent",
  "version": "0.1.0",
  "description": "PROOFAGENT — ZK Authorization Layer for Autonomous AI Agents",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:base": "hardhat run scripts/deploy_base_mainnet.js --network base",
    "deploy:base-sepolia": "hardhat run scripts/deploy_base_mainnet.js --network baseSepolia",
    "verify": "hardhat verify --network base",
    "test": "hardhat test",
    "clean": "hardhat clean"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "author": "Charly Grossrieder <hello@proofagent.ch>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Charlot74/proofagent"
  }
}
